import React, { Component } from 'react';
import { View, Text, StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    cardStyle: {
        padding: 10,
        backgroundColor: 'white',
        shadowColor: "#000000",
        flexDirection: 'row',
        width: '100%'
    },
    btnSubmit: {
        backgroundColor:'#2793E1',
        height: 40,
        alignItems: 'center',
        borderRadius: 5,
        justifyContent: 'center'
    },
})
